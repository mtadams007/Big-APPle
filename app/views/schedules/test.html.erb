<div class="container-fluid">
    <div class="row">

    <div class="schedule-layout-morning col-sm-6">
        <div class="time-of-day">Morning</div>

        <%@activity = @morning %>
  <%= render 'activity-cards' %>

  <%@activity = @lunch %>
  <%= render 'activity-cards' %>


   </div>
